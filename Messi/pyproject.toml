[project]
name = "cyberdog-soccer-rl"
version = "0.1.0"
description = "Headless RL training environment for the Cyberdog striker"
requires-python = ">=3.10"
dependencies = [
  "gymnasium",
  "numpy",
  "onnx",
  "onnxruntime",
  "onnxscript",
  "pyyaml",
  "stable-baselines3",
  "swanlab>=0.7.18",
  "tensorboard",
]

[tool.uv]
package = false
